
/*

When using the sizeof(type) in the denominator when determining the length of an array, you now how to account for an additional variable in case of changes to the arrays type rather than dynamically accessing the first element with a[0].

*/