/*

(i += 1) is equivalent to ++i.

++i is the prefix increment: it increases the value of i immediately and returns the new value, which makes it behave exactly like (i += 1).

i++ is the postfix increment: it also increases i by 1, but it returns the original value before the increment. The increment happens after the current expression is evaluated, so it does not yield the new value immediately.
*/
