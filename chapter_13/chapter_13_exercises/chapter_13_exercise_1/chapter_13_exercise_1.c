
/*
a) True
b) False, trying to insert a string (char *) into just a char print.
c) False, trying to insert a char into a string (char *).
d) True
e) False, printf expect a format string (char *) while we are trying to insert a char (int)
f) True
g) True
h) False, putchar requires a char parameter not a char *
i) False, puts requires const char * (string) as parameter not a char (int)
j) True
k) True, puts automatically adds \n to end of the string.

*/
#include <stdio.h>

int main(void) {

    // printf("%c", '\n');
    // printf("%c", "\n");
    // printf("%s", '\n');
    // printf("%s", "\n");
    // printf('\n');
    // printf("\n");
    // putchar('\n');
    // putchar("\n");
    // puts('\n');
    // puts("\n");
    // puts("");
}